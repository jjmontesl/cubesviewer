How to create a virtualenv for CubesViewer

1) In order to create a virtual env, use:

virtualenv --python=/usr/bin/python2.7 cv

2) Activate it:

source cv/bin/activate

3) Update or upgrade dependencies:

pip install -r requirements.txt


To use the environment you just need to use step 2.

